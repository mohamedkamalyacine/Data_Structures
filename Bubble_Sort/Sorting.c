#include <stdio.h>
#include <stdlib.h>
#include "linkedlist.h"
#include "Sorting.h"

void bubbleSort(Node* ptrNode)
{
    int counter, index;
    int sorted = 0;

    ptrNode = head;

/*    for(counter = 0; counter < size - 1 && !sorted; counter++)
    {
        sorted = 1;

        for(index = 0; index < size - 1 - counter; index++)
        {
            if(data[index] > data[index + 1])
            {
                Swap(&data[index], &data[index + 1]);
                sorted = 0;
            }
        }
    }*/
}
